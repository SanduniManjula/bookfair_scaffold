// GlobalStyles component is no longer needed as styles are handled by globals.css
// This component can be removed or kept for backward compatibility
export default function GlobalStyles() {
  return null;
}
