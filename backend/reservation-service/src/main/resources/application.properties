# Reservation Service Configuration
# Note: This is a placeholder service. Actual reservation logic is in user-auth-service.
# No database needed - this service is stateless

# Server Configuration
server.port=8082

# CORS Configuration
# Can be overridden by environment variable: CORS_ALLOWED_ORIGINS
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
