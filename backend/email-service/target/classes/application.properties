# Email Service Configuration
# No database needed for email service
# spring.datasource.url=jdbc:mysql://localhost:3306/bookfair_db?useSSL=false&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=Nipuni00@@
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# Email Configuration
# For Gmail: Use smtp.gmail.com, port 587, enable "Less secure app access" or use App Password
# For Outlook: Use smtp-mail.outlook.com, port 587
# For other providers: Check their SMTP settings
# These values can be overridden by environment variables:
# SPRING_MAIL_HOST, SPRING_MAIL_PORT, SPRING_MAIL_USERNAME, SPRING_MAIL_PASSWORD
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:your_email@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:your_app_password_or_password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

server.port=8083

# CORS Configuration
# Can be overridden by environment variable: CORS_ALLOWED_ORIGINS
# For multiple origins, use comma-separated values: http://localhost:3000,http://localhost:3001
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}