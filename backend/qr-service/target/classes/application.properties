# QR Service Configuration
# Note: This is a placeholder service. Actual QR code generation is in user-auth-service (QrUtil).
# No database needed - this service is stateless

# Server Configuration
server.port=8084

# CORS Configuration
# Can be overridden by environment variable: CORS_ALLOWED_ORIGINS
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
